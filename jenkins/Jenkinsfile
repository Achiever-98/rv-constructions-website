pipeline {
    agent any

    stages {

        stage('Print Workspace') {
            steps {
                sh '''
                    echo "Current Directory:"
                    pwd

                    echo ""
                    echo "Workspace Files:"
                    ls -la

                    echo ""
                    echo "App Folder:"
                    ls -la app
                '''
            }
        }

        stage('HTML Validation') {
            steps {
                sh '''
                htmlhint app/*.html --config lint/.htmlhintrc
                '''
            }
        }

        stage('CSS Validation') {
            steps {
                sh '''
                stylelint "app/css/*.css" \
                --config lint/.stylelintrc.json
                '''
                }
            }

    }
}